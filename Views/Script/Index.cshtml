

@{
    ViewData["Title"] = "Scripts";
    Layout = "_Layout";
}
<section class="is-hero-bar">
    <div class="flex flex-col md:flex-row items-center justify-between space-y-6 md:space-y-0">
        <h1 class="title">
            @ViewData["Title"]
        </h1>
    </div>
    <button class="button light" onclick="window.history.back()">Back</button>
</section>